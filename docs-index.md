# Documentation Index

The in-app Docs page should use `docs/docs-nav.json` as its navigation manifest and render Markdown from the `docs/` directory.

## Suggested Docs Page Features

- sidebar navigation
- Markdown rendering
- Mermaid diagram support
- search
- copy-code buttons
- section breadcrumbs
- last updated metadata later
