import fs from 'node:fs';
import assert from 'node:assert/strict';
const source = fs.readFileSync('src/rocket-easter-egg.ts', 'utf8');
assert.match(source, /recordRocketDiscovery\(\)/);
assert.match(source, /detail: \{ egg: 'rocket', completed: true/);
assert.match(source, /ROCKET DISCOVERY RETAINED/);
assert.match(source, /RETRYING AUTOMATICALLY/);
assert.match(source, /window.history.pushState\(\{ activeTab: 'arena', easterEgg: 'rocket' \}/);
assert.match(source, /addEventListener\('online', resume\)/);
assert.match(source, /addEventListener\('focus', resume\)/);
console.log('Rocket portal and retry hooks are wired.');
